# PngNote2

## getColumnIndex
## postDelayed, many delay
## pushed fragment not singleton

## adapter = new BookReaderItemsAdapter(getActivity(), meetingId, agendaId, _bookDir.getFilePath());
* getFilePath() return file not exists, it will crash
* when entering, check the folder exists

## https://github.com/outsbook/CanvasEditor

